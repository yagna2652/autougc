# CLAUDE.md

AutoUGC 2.0 — AI-powered UGC video generation pipeline (TikTok analysis → prompt → scene image → video).

## WORKFLOW

For every task:

1. Analyze the user's request.
2. Run `find docs/ -name "*.md" | sort` to see available documentation.
3. Read only the docs relevant to the current task.
4. Present a concise execution plan with todo items.
5. Once the user accepts, execute.
6. After completing work, suggest running `/refine` to capture learnings.

## KEY PATHS

- Pipeline core: `src/pipeline/nodes/` and `src/pipeline/utils/`
- Product config: `assets/products/keychain/config.json`
- Interaction library: `assets/interaction_library/index.json`
- API server: `api/server.py`, `api/routes/pipeline.py`
- Frontend: `web/`
- Test scripts: `scripts/`

## RULES

- Be concise. Sacrifice grammar for clarity.
- If something looks wrong, stop and flag it.
- Do not guess — use tools to verify.
- Do not edit this file. Update docs/ instead.
